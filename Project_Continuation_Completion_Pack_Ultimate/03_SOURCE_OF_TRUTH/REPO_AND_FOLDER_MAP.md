# REPO_AND_FOLDER_MAP

## Active repo(s)
`F:\prosjekter_MAIN\AttentionOS` with remote `origin`.

## Inactive repo(s)
None known.

## Branches that matter
`master` currently tracks `origin/master`.

## Working folders that matter
`public/`, `src/lib/`, `tests/`, `docs/`, `extension/`, `Project_Continuation_Completion_Pack_Ultimate/`.

## Folders that are duplicates or deprecated
Generated dependency folders and failed install folders: `node_modules/`, `.bad-node_modules*`, `.deps/`.

## What a new developer should open first
`status_bundle.txt`, `README.md`, `server.mjs`, `src/lib/domain.js`, `public/app.js`.
