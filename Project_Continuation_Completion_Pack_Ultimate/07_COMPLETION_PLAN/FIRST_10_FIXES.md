# FIRST_10_FIXES

## Fix 1
Commit this filled continuation pack.

## Fix 2
Update `status_bundle.txt` with 2026-06-25 state and pack location.

## Fix 3
Remove or archive failed dependency folders from local workspace after confirming no needed files are inside.

## Fix 4
Replace `.env.example` with current runtime variables only.

## Fix 5
Add JSON-file or SQLite persistence behind `src/lib/store.js`.

## Fix 6
Add API tests for all `/api/v1` routes.

## Fix 7
Add request validation helpers and consistent API error shape.

## Fix 8
Implement extension capture POST flow with explicit user consent.

## Fix 9
Add export/delete endpoints before real user data.

## Fix 10
Set up CI for lint, build and tests.
